apiVersion: v1
kind: Service
metadata:
  name: platformnpservice-srv
spec:
  type: NodePort
  selector: 
    app: paltformservice
  ports:
    - name: paltformservice
      protocol: TCP
      port: 80
      targetPort: 80